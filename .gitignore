# Legacy nếu còn file cũ
.env.docker
prod/.env
stg/.env

# Node (repo-root tooling)
node_modules/

# Local project maps (machine paths — never commit)
platform-repos.local.json
legacy-repos.local.json

# Docs site build artifacts (source lives in docs/)
docs/.vitepress/dist
docs/.vitepress/.temp
docs/public/openapi/openapi.yaml
docs/public/swagger

# Optional Kilo mirror (SSOT after init: .cursor/skills|rules|extracts)
.kilo/skills/
.kilo/instructions/
.kilo/extracts/
.kilo/command/

# CodeGraph local index (repo-owned; outside toolkit ignore contract)
.codegraph/
.codegraph-*/

# Toolkit-managed — also merged by platform-dna / codegenkit / processkit init.
# Do not hand-edit these blocks; re-run init to refresh.
.cursor/
.codegenkit/
.platform-dna/
.processkit/
.mcp.json
.claude.json
.claude/
.codex/
.hermes/
.gemini/
.kiro/
.kilocode/
opencode.jsonc
