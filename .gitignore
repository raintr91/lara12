# Legacy nếu còn file cũ
.env.docker
prod/.env
stg/.env

# Node (docs tooling at repo root)
node_modules/

# Local project maps (repo root SSOT)
platform-repos.local.json
legacy-repos.local.json
.cursor/platform-repos.local.json
.cursor/legacy-repos.local.json

# Per-feature harness state (examples committed in .harness/*.example.*)
.harness/progress.md
.harness/feature_list.json

# Docs site build artifacts (source lives in docs/)
docs/.vitepress/dist
docs/.vitepress/.temp
docs/public/openapi/openapi.yaml
docs/public/swagger

# Optional Kilo mirror (SSOT: .cursor/skills|rules|extracts)
.kilo/skills/
.kilo/instructions/
.kilo/extracts/
.kilo/command/
