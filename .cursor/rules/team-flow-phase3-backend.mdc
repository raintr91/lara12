---
description: Phase 3b — backend API spec/code workflow
globs: src/Modules/**/*,src/docs/features/**/*
alwaysApply: false
---

# Phase 3b — Backend

Router skill: `.cursor/skills/team-phase3-backend/SKILL.md`

- Step 1 `/api-spec`: phân tích portal spec/testcases → backend spec + OpenAPI YAML + mock data. Không code Laravel.
- Step 2 `/api-code`: đọc backend spec đã review + `src/make_help.md` → implement bằng `m:*` generators.
- Swagger/OpenAPI source là YAML, không dùng decorator trong Laravel.
- Verify code: `php artisan test --filter={Module}` khi có implement.
