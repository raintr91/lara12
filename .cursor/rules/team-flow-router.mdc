---
description: Team backend workflow router — opt-in commands
alwaysApply: false
---

# Team AI Flow (API)

**Opt-in** — `/api`, `/api-spec`, `/api-integration-spec`, `/api-update-spec`, `/grill-api-spec`, `/grill-integration-spec`, `/api-code`.

Integration (no Portal FE): `/api-integration-spec`, `/grill-integration-spec` — `.cursor/rules/team-flow-api-integration-spec.mdc`

→ Router: `.cursor/skills/api/SKILL.md`  
→ Doc: `docs/operational/TEAM-AI-BACKEND-WORKFLOW.md`  
→ Extracts: `.cursor/extracts/`

PHPUnit riêng (không diagram, không testcase): `/unit`, `/grill-unit` — `.cursor/rules/team-flow-unit.mdc`

One session = one command. Portal FE specs: `../portal/docs/features/{slug}/`. State: `.harness/progress.md`.
