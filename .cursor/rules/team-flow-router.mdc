---
description: Team backend workflow router — opt-in commands
alwaysApply: false
---

# Team AI Flow (API)

**Opt-in** — `/api`, `/api-spec`, `/grill-api-spec`, `/api-code`.

→ Router: `.cursor/skills/api/SKILL.md`  
→ Doc: `src/docs/TEAM-AI-BACKEND-WORKFLOW.md`  
→ Extracts: `.cursor/extracts/`

One session = one command. Portal FE specs: `../portal/docs/features/{slug}/`.
