---
description: Code-lane harness bias — gen/MCP first; app only when asked
alwaysApply: true
---

# Platform AI

- **Harness SSOT:** `.cursor/` trên **repo code đang mở**.
- **Gen / analyze / tags:** Artifactgraph MCP + `pnpm portal:gen` / `unit-gen` / `testcase:gen` — AI chỉ **gap** (`#needs-*`, Mo* chưa registry).
- **Skills code:** `/platform-base` · `/platform-mark` · `/wire` · `/test` · `/unit` · `/model` (+ `/api` trên BE/fullstack).
- **Không** author handbook/spec/plans trên code repo; không load docs-lane skills.
- Bảo trì harness: `/platform-ai`.
