# Harness Progress (API)

Handoff giữa session — session mới đọc `progress.md` trước, không replay chat.

## Template entry

```markdown
## YYYY-MM-DD — /api-spec|/api-update-spec|/grill-api-spec|/api-code — {slug}

**Done:** ...
**Verified:** (command + result, nếu có)
**Portal sync:** (portalRefs stale/synced, pendingTechDebt count)
**Next:** ...
**Blocked:** ...
```

---

## Latest

_Chưa có entry — copy file này thành `progress.md` khi bắt đầu feature._
