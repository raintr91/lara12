<?php

namespace $MODULE_NAMESPACE$\$STUDLY_NAME$\Http\Middleware;

use Closure;
use Illuminate\Http\Request;

class Middleware
{
    public function handle(Request $request, Closure $next)
    {
        // Override in module-specific middleware if needed.
        return $next($request);
    }
}
