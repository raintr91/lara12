openapi: 3.1.0
info:
  title: API Base
  version: 0.1.0
  license:
    name: Proprietary
    identifier: LicenseRef-Proprietary
  description: >
    Base OpenAPI fragment (health). Feature paths are merged by
    `pnpm openapi:render` from docs/features/**/02-openapi.yaml.
servers:
  - url: http://api.base.local
    description: Local API server
tags:
  - name: Health
    description: Local documentation smoke endpoint.
paths:
  /health:
    get:
      operationId: healthCheck
      summary: Health check placeholder
      tags:
        - Health
      security: []
      responses:
        '200':
          description: API is reachable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
              example:
                status: ok
        '500':
          description: Unexpected server error
        '404':
          description: Endpoint not found
components:
  schemas:
    HealthResponse:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          example: ok
