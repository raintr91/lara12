{
  "$schema": "https://github.com/raintr91/platform-dna/blob/main/templates/schemas/platform-repos.schema.json",
  "defaultGroup": "be",
  "harness": {
    "defaultByRole": {
      "backend": "be",
      "be": "be"
    },
    "syncPolicy": {
      "mode": "propose",
      "description": "Current BE repository only; package init merges only package-owned BE skill IDs."
    },
    "profiles": {
      "be": {
        "groups": [
          "be"
        ],
        "skills": [
          "platform-ai",
          "platform-base",
          "platform-mark",
          "api",
          "grill-api",
          "wire",
          "unit",
          "grill-unit"
        ],
        "note": "Laravel BE current repository only"
      }
    }
  },
  "groups": {
    "be": {
      "description": "Backend current repository",
      "primary": "api",
      "projects": [
        "api"
      ]
    }
  },
  "projects": {
    "api": {
      "root": ".",
      "role": "be",
      "stack": "laravel",
      "repo": "api",
      "skill": "api-base",
      "description": "Base Laravel 12 (BE)",
      "write": true
    }
  }
}
