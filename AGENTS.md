# Project Instructions

## Code lane

Skills: `/api` · `/wire` · `/unit` (+ grill-*) · `/platform-base` · `/platform-mark`  
Gen / gaps: **Artifactgraph MCP** (khi dùng từ FE product) hoặc runners trên repo này.

SSOT harness: `.cursor/` tại repo này.
